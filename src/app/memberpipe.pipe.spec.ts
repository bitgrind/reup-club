import { MemberpipePipe } from './memberpipe.pipe';

describe('MemberpipePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberpipePipe();
    expect(pipe).toBeTruthy();
  });
});
